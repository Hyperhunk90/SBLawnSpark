{
  "name": "southern-buck-lawn-care",
  "version": "1.0.0",
  "description": "Premium lawn care and landscaping services website",
  "type": "module",
  "scripts": {
    "dev": "vite dev --host --port 3000",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "pm2": "^6.0.8"
  },
  "devDependencies": {
    "vite": "^5.4.8",
    "wrangler": "^3.78.12"
  }
}
